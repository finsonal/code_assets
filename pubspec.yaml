name: myfinsonal_main

description: 多链路全栈广告群控系统，整合广告管理与代码资产功能；商哩 SAIL LINK 电商社交平台
  # 合并项目描述，整合两个版本的核心内容

publish_to: 'none' # 防止意外发布到 pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # logger: ^1.4.0  # 修复缩进并添加空格以符合YAML语法要求
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  # 修复私有仓库URL错误，使用pub.dev公共仓库
  http: ^1.4.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  # 合并本地logger依赖与远程仓库的更新版本依赖
  flutter_localizations:
    sdk: flutter
  image_picker: ^1.1.2
  path_provider: ^2.0.11
  sqflite: ^2.0.3
  url_launcher: ^6.1.5
  cached_network_image: ^3.2.1
  flutter_svg: ^2.0.10+1
  # 保留本地较新版本以支持高级SVG功能
  geolocator: ^12.0.0
  amap_flutter_map: ^3.0.0
  amap_flutter_location: ^3.0.0
  # amap_search_flutter: ^3.0.0  # 暂时注释，该包在pub.dev上不存在
  flutter_slidable: ^3.1.2
  flutter_staggered_grid_view: ^0.7.0
  flutter_rating_bar: ^4.0.1
  carousel_slider: ^5.1.1
  shimmer: ^3.0.0
  flutter_secure_storage: ^9.2.4
  dio: ^5.8.0+1
  socket_io_client: ^3.1.2
  web_socket_channel: ^3.0.3
  # 采用远程仓库的实时通讯依赖新版本
  encrypt: ^5.0.1
  pointycastle: ^3.7.4
  # flutter_local_notifications: ^9.7.0  # 暂时注释掉，因为与flutter_driver不兼容
  firebase_core: ^3.5.0
  firebase_messaging: ^15.2.0
  # 升级Firebase依赖以支持最新功能
  firebase_analytics: ^11.3.0
  firebase_crashlytics: ^4.1.0
  flutter_markdown: ^0.7.4+3
  photo_view: ^0.15.0
  video_player: ^2.4.5
  chewie: ^1.3.4
  flutter_html: ^3.0.0
  webview_flutter: ^4.13.0
  # 采用远程仓库的HTML渲染和WebView新版本
  flutter_stripe: ^11.5.0
  # flutter_paypal: ^0.0.8  # 暂时注释掉，因为与webview_flutter ^3.0.4版本冲突
  # flutter_alipay: ^0.1.2  # 暂时注释掉不支持空安全的包
  # wechat_pay: ^0.3.0  # 暂时注释掉不存在的包
  flutter_easyloading: ^3.0.5
  flutter_easyrefresh: ^2.2.1
  permission_handler: ^11.3.1
  package_info_plus: ^8.0.2
  # device_info_plus: ^4.1.2  # 暂时注释掉，因为与integration_test不兼容
  connectivity_plus: ^6.1.4
  flutter_native_splash: ^2.2.7
  flutter_launcher_icons: ^0.14.3
  # # # # # 已移除重复的flutter_dotenv依赖项 (重复项已移除) (重复项已移除) (重复项已移除) (重复项已移除)
  logger: ^2.5.0
  # 移除重复的logger依赖项
  get_it: ^7.2.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  json_annotation: ^4.6.0
  freezed_annotation: ^2.1.0
  dartz: ^0.10.1
  rxdart: ^0.28.0
  uuid: ^4.5.1
  flutter_screenutil: ^5.5.3+2
  flutter_spinkit: ^5.1.0
  # flutter_form_builder: ^7.7.0  # 暂时注释掉，因为与intl ^0.18.1版本冲突
  # form_builder_validators: ^8.3.0  # 暂时注释掉，因为依赖于flutter_form_builder
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.6.1
  flutter_riverpod: ^2.6.1
  riverpod: ^2.6.1
  auto_route: ^8.1.4
  injectable: ^2.4.2
  get: ^4.6.5
  flutter_gen: ^5.9.0
  geolocator_windows: ^0.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.2.0
  json_serializable: ^6.7.1
  freezed: ^2.1.0+1
  mockito: ^5.3.0
  # 升级开发依赖以支持最新代码生成功能
  auto_route_generator: ^8.0.0
  injectable_generator: ^2.6.1
  flutter_gen_runner: ^5.9.0
  integration_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: ^1.21.4

dependency_overrides:
  # 移除code_assets的Git依赖，改用本地管理
  # code_assets: ^1.0.0
  #   path: ../code_assets
  js: ^0.7.1
  # 保留JavaScript互操作支持

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    # - assets/icons/
    # - assets/animations/
    # - assets/data/
    - .env

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
    # - family: NotoSansSC
    #   fonts:
    #     - asset: assets/fonts/NotoSansSC-Regular.otf
    #     - asset: assets/fonts/NotoSansSC-Bold.otf
    #       weight: 700
    #     - asset: assets/fonts/NotoSansSC-Light.otf
    #       weight: 300

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/splash.png
  android: true
  ios: true

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/app_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"